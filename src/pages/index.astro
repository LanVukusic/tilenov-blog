---
import Layout from "../layouts/Layout.astro";
import OblacekButton from "../components/oblacekButton.astro";
import Oblacek from "../components/oblacek.astro";
import Nogice from "../components/nogice.astro";
import Content from "../components/content.astro";
import "@fontsource/reenie-beanie";
import "@fontsource/mansalva";
---

<Layout title="Tilenov blog">
  <main class="bg-blue-50">
    <div
      id="oblaki"
      class="w-full h-48 z-10"
      style="background: url(topoblakcek.png);  background-size: contain; position: fixed; top: 0; background-repeat: repeat-x;"
    >
    </div>
    <div
      style="margin-top: 100vh;"
      class="flex p-6 max-w-screen-lg mx-auto items-center justify-around mb-36"
    >
      <div style="transform: rotate(5deg);">
        <Oblacek />
      </div>
      <div class="flex flex-col">
        <h1 class="reenieBeenie text-5xl px-4 text-center">
          Pretresljivi zapisi Tilena Letnerja
        </h1>
        <div class="flex items-start" id="header">
          <div class="h-24 flex">
            <div
              class="w-8 h-8 flex"
              style="transform: rotate(-20deg); transform: translate(3rem);"
            >
              <img src="listek2.png" />
            </div>
          </div>
          <div class="manslava text-9xl text-amber-500 text-center">Moj bl</div>

          <div class="w-16 h-16 flex mt-12" style="">
            <img src="pomaranca.png" />
          </div>
          <div class="manslava text-9xl text-amber-500 text-center">g</div>
          <div class="flex mt-24">
            <div
              class="w-8 h-8 flex"
              style="transform: translate(-0.7rem)  rotate(20deg);"
            >
              <img src="listek1.png" />
            </div>
          </div>
        </div>
      </div>
      <div style="transform: rotate(160deg);">
        <Oblacek />
      </div>
    </div>

    <!-- navbar section -->

    <div
      class="w-full h-32"
      style="background: url(topoblakcek3.png);  background-size: contain; background-repeat: repeat-x; transform: rotate(180deg);"
    >
    </div>
    <div class="bg-white w-full py-6">
      <div class="flex max-w-screen-md mx-auto justify-center">
        <OblacekButton text="Jaz" active={false} />
        <OblacekButton text="O meni" active />
        <OblacekButton text="Več o meni" active={false} />
      </div>
    </div>
    <div
      class="w-full h-32"
      style="background: url(topoblakcek3.png);  background-size: contain; background-repeat: repeat-x;"
    >
    </div>

    <!-- content section -->

    <div class="bg w-full flex -mt-24 min-h-screen">
      <div class="mx-auto w-full max-w-screen-md mt-48 flex items-center">
        <Content
          title="Zgodba o marksu naslov"
          text="What?When scrolling past certain DOM elements, we want to alter their behaviour so that they transition from statically positioned to fixed positioned.A classic example is the sticky site navigation. We have some form of intro image/splash for our site and a navigation bar that sits beneath. If we didn’t implement the “sticky” behaviour, as we scroll through our content, we lose the navigation bar and this forces the user to scroll back to the top in order to navigate which isn’t ideal 👎The ideal user experience is to make the navigation available to the user at all times in a simple way. Don’t let it go out of the viewport and keep it stuck at the top of the viewport when applicable 👍"
        />
      </div>
    </div>

    <div class="h-64"></div>

    <Nogice />
  </main>
</Layout>

<style>
  .bg {
    background-image: url(/tereza.png);
    background-size: 400px;
    background-repeat: repeat;
    /* minheight: 100vh; */
  }

  .rainbow {
    background: #40e0d0; /* fallback for old browsers */
    background: -webkit-linear-gradient(
      to right,
      #ff0080,
      #ff8c00,
      #40e0d0
    ); /* Chrome 10-25, Safari 5.1-6 */
    background: linear-gradient(
      to right,
      #ff0080,
      #ff8c00,
      #40e0d0
    ); /* W3C, IE 10+/ Edge, Firefox 16+, Chrome 26+, Opera 12+, Safari 7+ */
  }

  .reenieBeenie {
    font-family: "Reenie Beanie", cursive;
  }

  .manslava {
    font-family: "Mansalva", cursive;
  }
</style>

<script>
  const oblaki = document.querySelector("#oblaki");

  document.addEventListener("scroll", (event) => {
    let ticking = false;

    if (!ticking) {
      window.requestAnimationFrame(() => {
        if (oblaki) {
          // @ts-ignore
          oblaki.style.opacity = `${1 - window.scrollY / 500}`;
        }
      });

      ticking = true;
    }
  });
</script>
